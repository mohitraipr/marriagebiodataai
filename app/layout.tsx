import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import Header from "@/components/Header";
import Footer from "@/components/Footer";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Marriage Biodata Maker | Free Biodata for Marriage | Kundli Milan",
  description: "Create beautiful marriage biodata in minutes. Free biodata maker with 8+ templates, 9 languages, Kundli Milan calculator. Download in PNG/PDF format instantly.",
  keywords: "marriage biodata, biodata for marriage, kundli milan, horoscope matching, biodata maker, matrimonial biodata",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${geistSans.variable} ${geistMono.variable} antialiased`}>
        <div className="min-h-screen flex flex-col">
          <Header />
          <main className="flex-1">{children}</main>
          <Footer />
        </div>
      </body>
    </html>
  );
}
